<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android_note_mvc">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/SplashTheme">
        <activity
            android:name=".activty.SearchActivity"
            android:exported="false" />
        <activity
            android:name=".activty.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activty.MainActivity"
            android:exported="false"
            android:theme="@style/Theme.Android_note_mvc" />
        <activity
            android:name=".activty.DetailActivity"
            android:exported="false"
            android:theme="@style/Theme.Android_note_mvc" />
    </application>

</manifest>